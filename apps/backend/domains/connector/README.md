# Connector

Owns external-provider adapters, connection and synchronization infrastructure, and the raw-ingestion boundary. Provider-specific representations stop here and are passed to Normalisation through contracts.

