# OpenAPI contract

The Django schema at `/api/schema/` is authoritative. Refresh the checked contract with `make schema` after an intentional API change. Client-specific generated code must never become the source of backend behavior.
